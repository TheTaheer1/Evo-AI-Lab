# Hugging Face dedicated inference endpoint (teacher, critic, verifier LLM paths)
# Copy to ".env" in this folder and fill with your endpoint URL and read token.
HF_API_URL=https://tr5nuqcszts71vfy.us-east-1.aws.endpoints.huggingface.cloud
# Never commit a real token. Put your token only in .env (gitignored).
HF_TOKEN=
